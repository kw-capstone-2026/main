# 프로젝트 진행 리포트 (2026-05-14)

## 1. 금일 작업 요약

팀원들이 개별적으로 가공한 데이터셋(DS1, DS5)을 확보하고, 이를 기존 베이스라인 데이터와 결합하여 **최종 분석용 통합 데이터셋(`final_merged_commercial_data.parquet`)**을 구축함.

## 2. 세부 수행 내역

### ① 데이터 확보 및 경로 정형화
- 팀원들이 공유한 파편화된 Parquet 파일들을 `data/parquet_datas/` 폴더로 집결 및 관리 체계 구축.

### ② 데이터 스키마 전수 조사 및 결함 발견
- **포맷 불일치:** `기준_년분기_코드`가 기존 데이터(정수형 `20231`)와 팀 데이터(문자열 `"2023Q1"`) 간에 충돌하는 문제 발견.
- **데이터 타입 오류:** 점포 데이터 내의 수치형 변수(개업률, 폐업률, 영업개월 등)가 문자열(str)로 저장되어 모델 학습이 불가능한 상태임을 확인.
- **리소스 낭비:** 5.2GB 규모의 `df_final.parquet` 파일 내에 학습에 불필요한 GIS 바이너리 데이터(Polygon WKB)가 포함되어 메모리 부하를 초래하는 문제 식별.

### ③ 자동화된 데이터 병합 파이프라인 구축 (`agenttest/merge_parquets.py`)
- **데이터 타입 강제 교정:** 문자열 형태의 수치 데이터들을 `Float64`로 일괄 변환.
- **시간 축 정규화:** `"2023Q1"` 형태의 코드를 정수형 `20231`로 치환하는 전처리 로직 적용.
- **메모리 최적화:** 대용량 파일 로드 시 필요한 컬럼만 선택적으로 로드하고, 불필요한 폴리곤 데이터를 드롭하여 효율적인 병합 수행.
- **결과:** 총 512,626행의 정합성이 확보된 통합 데이터셋 생성 완료.

### ④ 통합 데이터 품질 검사 및 리포팅 (`agenttest/inspect_merged_data.py`)
- **결측치 분석:** 추가된 유동인구 데이터의 약 93%가 결측치(Null)임을 확인. 원인은 팀 데이터의 시계열 범위(2023~2024)가 베이스라인(2019~2024)보다 좁기 때문으로 판명.
- **데이터 무결성:** 모든 조인 키(`상권_코드`, `기준_년분기_코드`)가 중복 없이 완벽하게 매핑되었음을 검증.

## 3. 향후 전략 및 대안

- **인구 데이터 결측치 보완:** 
    - (대안 A) 자치구/상권구분별 평균값을 활용한 그룹 기반 보간(Group Imputation).
    - (대안 B) 2020년 행정동별 생활인구 데이터를 확보하여 상권별 비중으로 역산(Top-down)하는 방식 검토.
    - (대안 C) 2020년 250m 격자 데이터 원본 확보 시 공간 연산을 통한 완벽한 데이터 복구.
- **모델 학습:** 통합 데이터셋을 활용하여 XGBoost 베이스라인 모델 성능 업데이트 및 피처 중요도 분석.

---
**기록자:** Antigravity (AI Assistant)
